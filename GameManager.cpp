#include "GameManager.h"

GameManager::GameManager()
{
    this->gameState = 1;
}

void GameManager::runGameLoop(int chapter, int level)
{
    //Read file- takes arguments "chapter" and "level" and uses them to create a string that represents the directory for the level
    //Display background & character @ spawn
    //Start loop:
        //Take input
        //Handle game logic
        //Update screen
}

void GameManager::runMenuLoop()
{
    
}